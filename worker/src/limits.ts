// Search budget limits, shared by the orchestrator and the adapters.
export const PER_SOURCE_TIMEOUT_MS = 6000;
export const TOTAL_BUDGET_MS = 8000;
export const MAX_PER_SOURCE = 20;
export const MAX_TOTAL = 60;
// Asset libraries get a small cap when the query is not asset-seeking.
export const ASSET_SITE_CAP = 4;
